input {
  kafka {
    bootstrap_servers => "192.168.56.101:9092"
    topics => ["zebra-log"]
  }
}

filter {
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    codec => json
    index => "zebra-%{+YYYY.MM.dd}"
  }
}